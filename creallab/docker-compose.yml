version: "3"

services:
  creallab:
    build: .
    ports:
      - "8081:8080"
    volumes:
      - ./:/srv/jekyll
    command: >
      bundle exec jekyll serve --host 0.0.0.0 --port 8080 --config _config.yml,_config_local.yml

